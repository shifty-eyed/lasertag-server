<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss:SSS} %highlight([%5.5level]) %12.12thread: %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="SSE_LOG" class="net.lasertag.lasertagserver.web.SseLogAppender">
    </appender>

    <!-- Capture all logs from UdpServer and Game classes -->
    <logger name="net.lasertag.lasertagserver.core.UdpServer" level="DEBUG" additivity="true">
        <appender-ref ref="SSE_LOG"/>
    </logger>

    <logger name="net.lasertag.lasertagserver.core.Game" level="DEBUG" additivity="true">
        <appender-ref ref="SSE_LOG"/>
    </logger>

    <logger name="net.lasertag" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>